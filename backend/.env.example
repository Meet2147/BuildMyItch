# Copy this file to .env and fill in your key.
# The apps' "smart" features (voice-to-site, payroll notes, restock forecasts)
# call the Claude API through this backend.

ANTHROPIC_API_KEY=sk-ant-your-key-here

# Change this to any random string before deploying anywhere real.
JWT_SECRET=change-me-to-something-random

# The email that is seeded as the super-admin (has every permission).
ADMIN_EMAIL=meetjethwa3@gmail.com

PORT=4000
